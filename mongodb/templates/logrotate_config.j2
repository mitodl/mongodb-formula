{{ name }} {
  {%- for item in options %}
    {% if item.startswith('prerotate') or item.startswith('postrotate') %}
    {%- set prefix, postfix = item.split(' ', 1) -%}
    {{ prefix }}
        {{ postfix }}
    endscript
    {%- else -%}
    {{ item }}
    {%- endif -%}
  {%- endfor %}
}
